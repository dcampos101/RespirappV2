<%= form_with(model: contamination_level, local: true) do |form| %>
  <% if contamination_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contamination_level.errors.count, "error") %> prohibited this contamination_level from being saved:</h2>

      <ul>
      <% contamination_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :contamination_level_nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :contamination_level_descripcion %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado, id: :contamination_level_estado %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
