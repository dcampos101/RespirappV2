<%= form_with(model: report_type, local: true) do |form| %>
  <% if report_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report_type.errors.count, "error") %> prohibited this report_type from being saved:</h2>

      <ul>
      <% report_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :report_type_nombre %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado, id: :report_type_estado %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
